  spring:
    application:
      name: Default Config Prod
  
  eureka:
    instance:
      instance-id: USER-SERVICE
      prefer-ip-address: true
    client:
      fetch-registry: true
      register-with-eureka: true
      service-url:
        defaultZone: http://localhost:8761/eureka
